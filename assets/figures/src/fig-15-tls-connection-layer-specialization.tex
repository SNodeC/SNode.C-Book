\documentclass[tikz,border=0pt]{standalone}
\input{snodec-figure-style.tex}
\usetikzlibrary{positioning,calc,arrows.meta}

\begin{document}

\def\majorVGap{11mm}
\def\streamRowGap{5mm}
\def\streamNoteGap{2.5mm}
\def\variantGap{5mm}

\begin{tikzpicture}[
  snodec diagram,
  role block/.style={
    draw=snodecGreen!70,
    fill=snodecGreenSoft,
    rounded corners=\snodecBoxCorner,
    line width=\snodecBoxLineWidth,
    align=center,
    text width=140mm,
    inner xsep=3mm,
    inner ysep=2.4mm,
    font=\sffamily\small
  },
  family block/.style={
    draw=snodecBlue!72,
    fill=snodecBlueSoft,
    rounded corners=\snodecBoxCorner,
    line width=\snodecBoxLineWidth,
    align=center,
    text width=140mm,
    inner xsep=3mm,
    inner ysep=2.4mm,
    font=\sffamily\small
  },
  stream block/.style={
    draw=snodecOrange!75,
    fill=snodecOrangeSoft,
    rounded corners=\snodecBoxCorner,
    line width=\snodecBoxLineWidth,
    align=center,
    text width=140mm,
    inner xsep=3mm,
    inner ysep=2.4mm,
    font=\sffamily\small
  },
  variant block/.style={
    draw=snodecOrange!55,
    fill=snodecOrangeSoft!45,
    rounded corners=\snodecBoxCorner,
    line width=\snodecBoxLineWidth,
    align=center,
    text width=58mm,
    inner xsep=2mm,
    inner ysep=2.4mm,
    font=\sffamily\footnotesize
  },
  context block/.style={
    draw=snodecRule!85,
    fill=snodecGraySoft,
    rounded corners=\snodecBoxCorner,
    line width=\snodecBoxLineWidth,
    align=center,
    text width=140mm,
    inner xsep=3mm,
    inner ysep=2.4mm,
    font=\sffamily\small
  },
  layer flow/.style={
    snodec secondary arrow,
    draw=snodecMuted!75,
    line width=\snodecBoxLineWidth
  },
  stream note/.style={
    font=\sffamily\footnotesize,
    text=snodecMuted,
    align=center
  }
]

% ----------------------------------------------------------------------
% Configured server/client role.
% ----------------------------------------------------------------------
\node[role block] (role) {%
  \textbf{\textcolor{snodecTitleGreen}{Server/client instance}}\\[-0.4mm]
  {\footnotesize configured communication role}\\[-0.2mm]
  {\footnotesize same application-side role: listen/connect, instance name, sections}
};

% ----------------------------------------------------------------------
% Endpoint family.
% ----------------------------------------------------------------------
\node[family block, below=\majorVGap of role] (family) {%
  \textbf{\textcolor{snodecTitleBlue}{Endpoint family}}\\[-0.4mm]
  {\footnotesize address and carrier family stay the same}\\[-0.2mm]
  {\footnotesize \texttt{net::in}, \texttt{net::in6}, \texttt{net::un}, \texttt{net::rc}, \texttt{net::l2}}
};

% ----------------------------------------------------------------------
% Stream connection form.
% This is an ordinary content node, not a fit panel. Its headline spacing
% follows the same inner-ysep logic as the other major boxes.
% ----------------------------------------------------------------------
\node[stream block, below=\majorVGap of family] (stream) {%
  \textbf{\textcolor{snodecTitleOrange}{Stream connection form}}\\[\streamRowGap]
  \begin{tikzpicture}[
    baseline=(legacyStream.base),
    variant row block/.style={variant block}
  ]
    \node[variant row block] (legacyStream) {%
      \textbf{\textcolor{snodecTitleOrange}{Legacy stream}}\\[-0.4mm]
      plain byte transport\\[-0.2mm]
      \texttt{ConfigLegacy}
    };

    \node[variant row block, right=\variantGap of legacyStream] (tlsStream) {%
      \textbf{\textcolor{snodecTitleOrange}{TLS stream}}\\[-0.4mm]
      encrypted byte transport\\[-0.2mm]
      \texttt{ConfigTlsClient} / \texttt{ConfigTlsServer}
    };
  \end{tikzpicture}\\[\streamNoteGap]
  {\footnotesize\textcolor{snodecMuted}{specialization point: connection setup, TLS context, certificates, SNI}}
};

% ----------------------------------------------------------------------
% Socket context.
% ----------------------------------------------------------------------
\node[context block, below=\majorVGap of stream] (context) {%
  \textbf{\textcolor{snodecTitleGray}{Socket context}}\\[-0.4mm]
  {\footnotesize protocol behavior attached to the established stream connection}\\[-0.2mm]
  {\footnotesize HTTP, WebSocket, MQTT, custom protocol contexts}
};

% ----------------------------------------------------------------------
% Layer arrows.
% ----------------------------------------------------------------------
\draw[layer flow] (role.south) -- (family.north);
\draw[layer flow] (family.south) -- (stream.north);
\draw[layer flow] (stream.south) -- (context.north);

\snodecApplyFixedCanvas

\end{tikzpicture}
\end{document}
