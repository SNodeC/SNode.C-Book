\documentclass[tikz,border=0pt]{standalone}
\input{snodec-figure-style.tex}
\usetikzlibrary{positioning,calc,arrows.meta}
\begin{document}
\begin{tikzpicture}[
  snodec diagram,
  scope box/.style={snodec runtime box,text width=66mm,minimum height=37mm},
  record box/.style={snodec protocol box,text width=66mm,minimum height=37mm},
  wide box/.style={snodec neutral box,text width=142mm},
  output box/.style={snodec application box,text width=66mm,minimum height=21mm}
]
\node[scope box] (scope) {\textbf{Semantic scope}\\[1.5mm]
  origin: framework or application\\
  boundary: owned responsibility\\
  component: diagnostic subsystem\\[1mm]
  optional identity:\\instance, server/client role, connection};
\node[record box,right=4mm of scope] (event) {\textbf{Observed occurrence}\\[1.5mm]
  severity and message\\
  optional stable event name\\
  optional typed system error\\[1mm]
  A queued operation is not yet\\a peer-confirmed result.};
\coordinate (mid) at ($(scope.south)!0.5!(event.south)$);
\node[wide box,below=14mm of mid] (policy) {\textbf{Startup threshold policy}\\[1.5mm]
  instance $\rightarrow$ component $\rightarrow$ boundary
  $\rightarrow$ origin $\rightarrow$ global\\[1mm]
  First applicable override selects the threshold; not five cumulative gates.};
\draw[snodec main arrow] (scope.south) -- ++(0,-6mm) -| (policy.north);
\draw[snodec main arrow] (event.south) -- ++(0,-6mm) -| (policy.north);
\node[output box,below=12mm of policy.south west,anchor=north west] (text) {
  \textbf{Text presentation}\\[1mm]
  attributed lifecycle and diagnostics\\
  readable without terminal color};
\node[output box,right=4mm of text] (json) {
  \textbf{JSON presentation}\\[1mm]
  structured record fields\\
  no parsing of English messages};
\draw[snodec main arrow] (policy.south) -- ++(0,-6mm) -| (text.north);
\draw[snodec main arrow] (policy.south) -- ++(0,-6mm) -| (json.north);
\node[snodec note,text width=140mm,below=7mm of text.south east,anchor=north] (note) {
  Public application facade: \texttt{<Log.h>} / \texttt{snode::log}.\\
  Existing context helpers retain their object-owned application/framework scopes.};
\snodecApplyFixedCanvas
\end{tikzpicture}
\end{document}
