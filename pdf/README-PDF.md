# PDF status

The full manuscript PDF was intentionally omitted from this package because the Markdown manuscript was changed after the last available PDF build. Rebuild the PDF from the Markdown sources before distribution.
